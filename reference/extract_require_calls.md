# Extract require() Calls

Extract require() Calls

## Usage

``` r
extract_require_calls(lines)
```

## Arguments

- lines:

  Character vector of code lines.

## Value

Character vector of package names

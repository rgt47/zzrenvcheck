# Validate Package on GitHub

Checks if a package exists on GitHub. Expects format "owner/repo".

## Usage

``` r
validate_github(package)
```

## Arguments

- package:

  Character. Package identifier in "owner/repo" format.

## Value

Logical. TRUE if repository exists on GitHub.

# Find R Files in Directories

Recursively finds R source files in specified directories.

## Usage

``` r
find_r_files(dirs, path = ".")
```

## Arguments

- dirs:

  Character vector of directory names.

- path:

  Character. Base path.

## Value

Character vector of file paths
